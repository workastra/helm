{{- if and .Values.iam.enabled .Values.iam.httpRoute.enabled }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "workastra.iam.fullname" . }}
  labels:
    {{- include "workastra.iam.labels" . | nindent 4 }}
spec:
  parentRefs:
    {{- .Values.iam.httpRoute.parentRefs | toYaml | nindent 4 }}
  hostnames:
    {{- .Values.iam.httpRoute.hostnames | toYaml | nindent 4 }}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /iam
      backendRefs:
        - name: {{ include "workastra.iam.fullname" . }}
          port: {{ .Values.iam.service.port }}
          weight: 1
{{- end }}
