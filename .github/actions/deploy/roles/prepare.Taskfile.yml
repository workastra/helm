# yaml-language-server: $schema=https://taskfile.dev/schema.json

version: '3'

tasks:
  ns:
    label: Create a namespace
    cmds:
      - cmd: kubectl get ns "{{ .TENANT_NAME }}" 2>/dev/null || kubectl create ns "{{ .TENANT_NAME }}"
