apiVersion: v1
kind: Service
metadata:
  name: {{ include "migration.fullname" . }}
  labels:
    {{- include "migration.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "migration.selectorLabels" . | nindent 4 }}
